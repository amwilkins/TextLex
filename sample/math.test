# This is not a comment
# Comments start with "//"
// I'm too indecisive to choose a comment style so this also works
(((s)))
test_string = "Hello, Compiler!";

1 <= 2;

var in = 1;

var out = in + 1;

print(out);
